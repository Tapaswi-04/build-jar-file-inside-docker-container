apiVersion: v1
kind: Service
metadata:
    name: tapaswibuildjarfileinsidedockercontainer
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: tapaswibuildjarfileinsidedockercontainer